/* 操作系统 · 解题套路库 —— 数据与展示分离，OS_套路库.html 读取本文件。
   来源：Claude 把王道课后题逐题做完后自己提炼的「看到什么 → 想到什么」，不是书上抄的。
   字段（与数构 data_tricks.js / 组原 data_tricks_co.js 完全一致，页面共用一套渲染）：
     id / ch / tier（🔴必考 🟡常考 🟢冷门但会考）/ title / trig[] / flow / tmpl / traps[] / qids[]
   ⚠️ 表格类名必须是 table.tk（.qtab 的样式作用域限定在 .q 之下，本页拿不到，会渲染成裸表）。
*/
window.TRICK_DATA = [

/* ==================== 第 1 章 计算机系统概述 ==================== */

{id:'TO1-1', ch:1, tier:'🔴', title:'题里出现「发生」还是「执行」——先圈时间副词，再答态',
 trig:['不可能在用户态发生','在用户态执行','发生时','开始执行时','处于内核态'],
 flow:'<b>OS 第 1 章最贵的两个字，2012 与 2024 两道真题都死在这儿。</b>做法只有一步：'
     +'<b>把题干里的时间副词圈出来</b>（发生 / 执行 / 开始执行时 / 返回后），再去查下表。<br>'
     +'口诀：<b>「敲门在门外，干活在门里」</b>——'
     +'系统调用、外中断、缺页都是<u>在用户态敲的门</u>（发生），<u>进了内核才干活</u>（执行）；'
     +'<b>只有进程切换是内核自己关起门来干的，门外根本看不见。</b>',
 tmpl:'<table class="tk"><tr><th>事件</th><th><b>发生</b>在哪一态</th><th><b>处理/执行</b>在哪一态</th></tr>'
     +'<tr><td>系统调用</td><td><b>用户态</b>（用户程序敲的）</td><td>内核态（服务例程）</td></tr>'
     +'<tr><td>外中断</td><td><b>用户态</b>（正跑用户程序时来的）</td><td>内核态（中断服务程序）</td></tr>'
     +'<tr><td>缺页</td><td><b>用户态</b>（用户程序访存时）</td><td>内核态（缺页服务程序）</td></tr>'
     +'<tr><td><b>进程切换</b></td><td><b>内核态</b>（调度器决定）</td><td>内核态</td></tr></table>'
     +'<p class="note"><b>2024 又把这条缝切得更细</b>：「中断或异常<u>发生时</u> CPU 处于内核态」<b>错</b>'
     +'（可能在用户态，也可能已在内核态——多重中断时被打断的就是内核代码）；'
     +'「中断处理程序<u>开始执行时</u> CPU 处于内核态」<b>对</b>。</p>',
 traps:['<b>「进程切换」为什么连发生都在内核态</b>：切换要存/取 PCB、改页表基址寄存器、换内核栈，'
       +'每一步都是特权指令。用户程序能做的只有「主动阻塞自己（系统调用）」或「被时钟中断打断」'
       +'——<b>两者都是先进内核，再由内核决定切不切</b>。',
       '<b>别把「模式切换」和「进程切换」当成一回事</b>：系统调用完通常还回到原进程，'
       +'只发生了模式切换、没发生进程切换。<b>这是 ch2 的高频陷阱，ch1 先埋在这里。</b>'],
 qids:['1.3-17','1.3-33','1.3-16','1.3-10']},

{id:'TO1-2', ch:1, tier:'🔴', title:'问「这一步由谁完成」——背那句十四字口诀，三年真题通吃',
 trig:['由操作系统完成的是','由硬件完成','应该由操作系统保存的','中断隐指令','保存断点'],
 flow:'<b>2015、2020、2022 三年考的是同一把刀</b>，答案彼此印证，一次记牢管三年。<br>'
     +'<b>口诀：断点、状态字、模式位 —— 硬件包办；<br>'
     +'　　　通用寄存器、屏蔽字、服务例程、向量表初始化 —— OS 负责。</b><br>'
     +'判据（不用死记）：<b>切换的那一瞬间软件还插不进手 ⟹ 归硬件；</b>'
     +'<b>「存几个、怎么存」取决于实现策略 ⟹ 归 OS。</b>'
     +'这正是「机制在硬件、策略在软件」的一个实例。',
 tmpl:'<table class="tk"><tr><th>阶段</th><th>动作</th><th>谁做</th></tr>'
     +'<tr><td rowspan="3">中断响应<br>（中断隐指令）</td><td>关中断</td><td><b>硬件</b></td></tr>'
     +'<tr><td><b>保存断点：PC + PSW</b></td><td><b>硬件</b></td></tr>'
     +'<tr><td><b>改模式位为内核态</b>、查中断向量表取入口</td><td><b>硬件</b></td></tr>'
     +'<tr><td rowspan="3">中断处理<br>（服务程序）</td><td><b>保存中断屏蔽字</b></td><td><b>操作系统</b></td></tr>'
     +'<tr><td><b>保存各通用寄存器</b></td><td><b>操作系统</b></td></tr>'
     +'<tr><td><b>提供中断服务 / 执行系统调用服务例程</b></td><td><b>操作系统</b></td></tr>'
     +'<tr><td>开机初始化</td><td><b>建立并初始化中断向量表</b></td><td><b>操作系统</b></td></tr>'
     +'<tr><td>不必保存</td><td>TLB（快表）、Cache</td><td>直接置有效位为 0</td></tr></table>',
 traps:['<b>TLB 与 Cache 不用保存</b>——它们是<u>可再生的副本</u>（页表和主存里有权威数据），'
       +'而通用寄存器里的值一丢就回不来。<b>2015 真题的 C、D 两个选项就是照这条设的。</b>',
       '<b>「初始化中断向量表」不发生在中断时，而在开机引导阶段</b>。'
       +'<b>2022 真题（1.6-20）单独问「OS 初始化过程中需要创建什么」，正解就是中断向量表。</b>'],
 qids:['1.3-21','1.3-27','1.3-31','1.6-20']},

{id:'TO1-3', ch:1, tier:'🔴', title:'判「这条指令在哪一态」——套三族表，两个特例单独记',
 trig:['可以在用户态执行','必须在内核态执行','只能在内核态执行','特权指令','不能在用户态执行'],
 flow:'<b>ch1 考了 5 次的单点（2014、2021 + 三道书内题）。</b>把散在书上三处的判据合成一张表，逐条对号即可。<br>'
     +'<b>一句话判据：这条指令会不会影响<u>整个系统</u>的安全与状态？会 ⟹ 特权（内核态）；'
     +'只动自己的数据 ⟹ 非特权（用户态）。</b>',
 tmpl:'<table class="tk"><tr><th>类别</th><th>典型指令</th><th>态</th></tr>'
     +'<tr><td><b>① 直接管理系统资源</b></td><td>置时钟 · 启停设备 · <b>I/O 指令</b> · 切换进程 · 设置中断</td>'
     +'<td rowspan="3"><b>内核态<br>（特权）</b></td></tr>'
     +'<tr><td><b>② 修改系统状态</b></td><td>改中断向量表 · 改模式位 · <b>修改 PSW</b> · <b>开/关中断</b> · <b>设中断屏蔽字</b></td></tr>'
     +'<tr><td><b>③ 系统控制</b></td><td><b>停机</b> · 重启</td></tr>'
     +'<tr><td><b>普通指令</b></td><td>算术逻辑 · 数据传送（存数/取数）· 转移跳转 · 压栈 · 寄存器清零 · <b>陷入（访管/Trap）指令</b></td>'
     +'<td><b>用户态<br>（非特权）</b></td></tr></table>'
     +'<p class="note"><b>两个必须单独记的特例：</b><br>'
     +'① <b>陷入/访管指令在用户态执行</b>——若它需要内核态，用户就永远进不去内核了（逻辑自洽，不用背）；<br>'
     +'② <b>中断屏蔽字设置指令在内核态</b>——链条较长：'
     +'<u>屏蔽字寄存器 ∈ 中断控制器 ∈ I/O 接口 ⟹ 属 I/O 指令 ⟹ 特权</u>。</p>',
 traps:['<b>「会导致进内核」≠「必须在内核里才能执行」</b>：Trap 指令、数据传送指令（可能缺页）、'
       +'设置断点指令都<u>在用户态执行</u>，只是执行后可能把 CPU 送进内核。'
       +'<b>2015 考前者（哪条不可能导致进内核态 ⟹ NOT R0），2021 考后者（哪条只能在内核态执行 ⟹ I/O 指令），'
       +'选项风格几乎一样，一不留神就答反。</b>',
       '<b>「管理员权限」不是「内核态」</b>：root 跑的程序照样在用户态，照样执行不了特权指令。'
       +'用户身份权限由 OS 软件判定，CPU 特权级由模式位硬件强制。'],
 qids:['1.3-08','1.3-09','1.3-20','1.3-28','1.3-22','1.3-30']},

{id:'TO1-4', ch:1, tier:'🔴', title:'「这是中断还是异常」——看事件源在 CPU 内还是外，再定返回哪条指令',
 trig:['属于中断','属于异常','内中断','外中断','缺页','除以零','DMA 传送结束','时钟中断'],
 flow:'<b>两步走：</b><br>'
     +'<b>① 定分类</b>：事件由<u>正在执行的这条指令</u>引发 ⟹ <b>异常（内中断）</b>；'
     +'由 <u>CPU 外部器件</u>送来 ⟹ <b>中断（外中断）</b>。<br>'
     +'<b>② 定返回位置</b>：<b>异常回第 i 条（重新执行），中断回第 i+1 条（继续往下）。</b><br>'
     +'自检口诀：<b>异常与指令<u>同步</u>（同一条指令必然重现），中断与指令<u>异步</u>（时机不定）。</b>',
 tmpl:'<pre class="code">中断 ┬ 异常（内）┬ 故障 Fault：缺页、除 0、非法操作码、溢出\n'
     +'     │            ├ 自陷 Trap ：访管/系统调用、条件陷阱指令\n'
     +'     │            └ 终止 Abort：控制器出错、存储器校验错\n'
     +'     └ 外中断（硬件）┬ 可屏蔽 INTR：一般外设请求、时钟中断\n'
     +'                     └ 不可屏蔽 NMI：掉电等紧急硬件故障\n\n'
     +'软件中断 = 故障 + 自陷 ； 硬件中断 = 终止 + 外中断\n'
     +'⚠️ 能被屏蔽的只有「可屏蔽中断」；NMI 与<b>所有异常</b>都屏蔽不掉</pre>'
     +'<p class="note"><b>组原口诀可直接搬来</b>：<b>故障退回重做、自陷与中断都往下走、终止直接不走了。</b></p>',
 traps:['<b>「缺页中断」名字带中断，分类却是异常（故障）</b>——2016 真题的错项就是这么设的，'
       +'1.3 节书内题 12 又考了一遍。<b>访管中断、陷阱中断同理，全是异常。</b>',
       '<b>「故障返回原指令重执行」不是记忆点，是虚存能成立的前提</b>：'
       +'缺页处理完必须<u>重新执行那条访存指令</u>，否则程序拿到垃圾数据。<b>ch3 请求分页全靠这条。</b>'],
 qids:['1.3-23','1.3-12','1.3-19','1.3-22']},

{id:'TO1-5', ch:1, tier:'🟡', title:'看到 sin()、strlen()、random() —— 一律「不进内核」',
 trig:['库函数','sin() 函数调用','生成随机整数','系统调用的区别','printf'],
 flow:'<b>ch1 考了四次的同一刀（2010、2013、2021 + 书内题）。</b>判据一句话：'
     +'<b>这个函数要不要碰「共享资源」（I/O、进程、内存）？</b>'
     +'要 ⟹ 它内部会触发系统调用；纯计算 ⟹ 全程用户态跑完。',
 tmpl:'<table class="tk"><tr><th>函数</th><th>进不进内核</th><th>怎么进</th></tr>'
     +'<tr><td><code>printf()</code></td><td><b>会</b></td><td>最终通过 <b>write</b> 系统调用</td></tr>'
     +'<tr><td><code>malloc()</code></td><td><b>可能会</b></td><td>要更多内存时调 <b>brk / mmap</b></td></tr>'
     +'<tr><td><code>strlen() sqrt() sin() random() abs()</code></td><td><b>不会</b></td><td>纯计算，用户空间内完成</td></tr></table>'
     +'<table class="tk"><tr><th></th><th>运行态</th><th>开销</th><th>可移植</th><th>可替换/可调试</th></tr>'
     +'<tr><td>库函数</td><td>用户态</td><td>小</td><td><b>好</b></td><td><b>容易</b></td></tr>'
     +'<tr><td>系统调用</td><td>内核态</td><td>大</td><td>差（与 OS 紧耦合）</td><td><b>麻烦</b></td></tr></table>',
 traps:['<b>「所有库函数都依赖系统调用」是错的</b>——举 strlen 一个反例即倒。'
       +'凡见「所有 / 任何 / 一定」这类全称量词，先找反例。',
       '<b>「库函数不方便被替换、系统调用很方便被替换」是说反了</b>（1.3-13 的 C）。'
       +'真相：系统调用是<u>内核 ABI</u>，一改所有已编译程序全崩（Linux 铁律 never break userspace）；'
       +'库函数换个 .so 就完事。'],
 qids:['1.1-11','1.1-10','1.3-19','1.3-29','1.3-13']},

{id:'TO1-6', ch:1, tier:'🔴', title:'系统调用：四步顺序背死，「谁发起」定归属',
 trig:['执行系统调用的过程','正确的执行顺序','通过系统调用完成的操作','陷入指令','系统调用号'],
 flow:'<b>两个考法，分别对应两句话：</b><br>'
     +'<b>① 问顺序</b>（2017）：<b>③传参 → ②陷入 → ④服务例程 → ①返回</b>。'
     +'记法：<u>先把货备好，再敲门，人家干活，最后送客</u>。<br>'
     +'<b>② 问「哪个操作通过系统调用完成」</b>（2021）：判据是'
     +'<b>「必须由<u>用户进程主动请求</u>」</b>——OS 自发的动作（页置换、进程调度）不算，'
     +'纯用户态的计算（random()）也不算。',
 tmpl:'<pre class="code">① 传递系统调用参数   —— 压栈/放寄存器（仍在用户态）\n'
     +'② 执行陷入指令       —— 用户态→内核态；硬件存 PC/PSW\n'
     +'③ 定位并执行服务程序 —— 按系统调用号查系统调用表取入口\n'
     +'④ 恢复现场返回用户态 —— IRET，从断点继续</pre>'
     +'<table class="tk"><tr><th>五大类系统调用</th><th>例</th></tr>'
     +'<tr><td>设备管理</td><td>请求/释放/启动设备</td></tr>'
     +'<tr><td>文件管理</td><td>读、写、创建、删除</td></tr>'
     +'<tr><td><b>进程控制</b></td><td><b>创建（fork）</b>、撤销、阻塞、唤醒</td></tr>'
     +'<tr><td>进程通信</td><td>消息传递、信号传递</td></tr>'
     +'<tr><td>内存管理</td><td>分配、回收、查询作业内存区大小与起址</td></tr></table>',
 traps:['<b>参数必须在陷入<u>之前</u>传</b>：陷入后内核第一件事就是按系统调用号查表，'
       +'参数没备好它无从下手。<b>2017 的三个错项全是把传参排到了陷入之后。</b>',
       '<b>页置换、进程调度不是系统调用</b>——它们由 OS 自己发起。'
       +'<b>但「申请内存（malloc→brk）」是系统调用</b>：申请是用户发起的，置换是 OS 自己决定的。',
       '<b>不同 OS 的系统调用接口<u>不统一</u></b>（2019 的错项 Ⅲ）：Linux 是 read/write/fork，'
       +'Windows 是 ReadFile/WriteFile/CreateProcess。<b>统一的是标准库层（POSIX/C 库）。</b>'],
 qids:['1.3-24','1.3-29','1.3-26','1.3-03']},

{id:'TO1-7', ch:1, tier:'🟡', title:'「并发」还是「并行」——先问是<u>谁和谁</u>并行',
 trig:['并行执行','并发性是指','同一时刻','同一时间间隔','多处理器','真正的并行'],
 flow:'<b>ch1 出现五次的最高频概念（1.1-05、1.2-14、2018 真题、1.6-16、1.7 疑难点）。</b><br>'
     +'<b>第一步贴标签：同一<u>时刻</u> ＝ 并行；同一<u>时间间隔</u> ＝ 并发。</b><br>'
     +'<b>第二步问层次</b>（这才是真正的分水岭）：',
 tmpl:'<table class="tk"><tr><th>谁和谁</th><th>单 CPU 上能否并行</th><th>依据</th></tr>'
     +'<tr><td><b>进程与进程</b></td><td><b>不能</b>（必须多处理器）</td><td>单 CPU 任一时刻只有一道程序在执行</td></tr>'
     +'<tr><td><b>CPU 与 I/O 设备</b></td><td><b>能</b></td><td>中断方式下 CPU 与外设<b>真并行</b>（书上原话）</td></tr>'
     +'<tr><td><b>多个 I/O 设备之间</b></td><td><b>能</b></td><td>同上，属硬件并行</td></tr></table>'
     +'<p class="note"><b>2018 真题的精妙处</b>：Ⅰ「多任务 OS 具有并发和并行的特点」<b>对</b>（并行来自 CPU 与外设那一层），'
     +'Ⅲ「需要运行在多 CPU 硬件上」<b>错</b>。<b>「具有并行特点」≠「必须多 CPU」。</b></p>',
 traps:['<b>虚拟机也栽在这条上</b>：多台虚拟机跑在同一物理机 ＝ 多个程序跑在同一系统 ⟹ <b>并发，不是真并行</b>。',
       '<b>2016 真题里「中断技术使 I/O 设备可与 CPU <u>并行</u>工作」用「并行」是对的</b>——'
       +'别一看到"并行"就以为必须多 CPU。'],
 qids:['1.1-05','1.2-14','1.2-19','1.6-16']},

{id:'TO1-8', ch:1, tier:'🟡', title:'问「某类系统追求什么目标」——批处理要多、分时要快、实时要准',
 trig:['追求的目标','主要追求目标','主要缺点是','必须在','内处理来自外部的事件'],
 flow:'三代系统的目标各一句，本节所有"目标/缺点"题都能一击即中：<br>'
     +'<b>批处理要「多」（吞吐量、资源利用率）· 分时要「快」（响应时间 T≈QN）· 实时要「准」（截止期限、确定性）。</b>',
 tmpl:'<table class="tk"><tr><th>系统</th><th>追求</th><th>主要缺点</th><th>关键机制</th></tr>'
     +'<tr><td>多道批处理</td><td>吞吐量、利用率</td><td><b>缺乏交互能力</b></td><td>依赖<b>中断技术</b></td></tr>'
     +'<tr><td>分时</td><td><b>及时响应交互</b></td><td>不保证时限</td><td>时间片轮转</td></tr>'
     +'<tr><td>实时</td><td><b>确定性、可靠性、时效性</b></td><td>牺牲资源利用率</td><td>按<b>紧迫性/截止时间</b>调度</td></tr></table>'
     +'<p class="note"><b>书答里最值钱的一句</b>：实时系统「为了保证快速处理高优先级任务，<u>允许"浪费"一些系统资源</u>」'
     +'⟹ <b>资源利用率不是实时系统的目标</b>。</p>',
 traps:['<b>「批处理的主要缺点」是常考点，书上点名要敏感</b>：是<b>缺乏交互性</b>，'
       +'不是"吞吐量小""CPU 利用率不高""需要大量内存"（这些全是错项）。',
       '<b>实时的时限由<u>被控制对象</u>规定</b>，不是"一个机器周期""周转时间""时间片"。',
       '<b>硬实时 vs 软实时</b>：超时会造成<u>永久性损害</u>的是硬实时（机床控制、导弹制导）；'
       +'只造成经济损失的是软实时（飞机订票、股票交易、银行系统）。'],
 qids:['1.2-06','1.2-10','1.2-02','1.2-05','1.2-07','1.2-15']},

{id:'TO1-9', ch:1, tier:'🟡', title:'时间片/响应时间计算：T ≈ Q × N，先统一单位再列式',
 trig:['响应时间','时间片最大','就绪进程数为','不超过 2s','用户数越多'],
 flow:'<b>ch1 唯一要动笔算的选择题类型。</b>公式只有一条：'
     +'<b>响应时间 T ≈ Q × N</b>（Q＝时间片，N＝用户/就绪进程数）。<br>'
     +'含义：轮一圈才轮回到你，一圈要走 N 个人、每人 Q 秒。<br>'
     +'<b>求时间片上界就反解：Q ≤ T / N。</b>',
 tmpl:'<pre class="code">例：就绪进程 100，要求响应不超过 2s\n'
     +'    Q ≤ T / N = 2 s / 100 = 0.02 s = 20 ms   ✔\n\n'
     +'干扰项是怎么造的：\n'
     +'  10ms ── 多留一倍余量 / 误用 T/(2N)\n'
     +'  50ms ── 把进程数记成 40\n'
     +' 100ms ── 直接把「进程数 100」当成了毫秒数（读题漏条件）</pre>',
 traps:['<b>单位</b>：题干给秒、选项给毫秒，算完必须 ×1000。<b>OS 全科的时间题都在 s/ms/μs 之间横跳</b>'
       +'（ch2 周转时间、ch5 磁盘寻道），养成「先统一单位再列式」的习惯。',
       '<b>方向别记反</b>：Q 固定时 N↑ ⟹ T↑；N 固定时 Q↑ ⟹ T↑。'
       +'所以<b>「加大时间片」会<u>延长</u>响应时间</b>（1.2-09 的错项 A）。'],
 qids:['1.2-12','1.2-11','1.2-09']},

{id:'TO1-10', ch:1, tier:'🔴', title:'甘特图排程题：设备独占 + 先到先得，先画图再算利用率',
 trig:['画出它们的甘特图','CPU 的利用率是多少','有无等待现象','单道程序环境','多道程序环境'],
 flow:'<b>规矩只有一条：同一台设备（CPU / 打印机 / 输入设备）任一时刻只能被一个程序占用，后到的排队。</b><br>'
     +'<b>四步：</b>① 按题给顺序把先执行的那个程序的各段铺满时间轴；'
     +'② 把第二个程序往空隙里塞，遇到设备冲突就往后推；'
     +'③ <b>CPU 利用率 = CPU 忙的总时长 ÷ 全部结束的时刻</b>；'
     +'④ 分别看「CPU 空闲区间」与「各进程的等待区间」——<b>这是两件事</b>。',
 tmpl:'<pre class="code">书上给的画法（可直接照抄进卷面）：\n'
     +'① 横坐标标时间刻度，纵坐标写程序名\n'
     +'② 过每个时间点向上作垂直虚线\n'
     +'③ 用「直线/波浪线/虚线」三种线区分对不同资源的占用\n\n'
     +'单道：总时间 = 各程序全长之和（完全串行）\n'
     +'多道：总时间 = 甘特图上最后一段的结束时刻</pre>'
     +'<table class="tk"><tr><th>题</th><th>结论（Python 排程复算，与书图逐段吻合）</th></tr>'
     +'<tr><td>1.2-21</td><td><b>单道 40/80 = 50% ； 多道 40/45 ≈ 88.9%</b></td></tr>'
     +'<tr><td>1.2-22</td><td><b>CPU 空闲仅 [100,150]ms，利用率 250/300 ≈ 83.3%</b>；A 无等待；'
     +'<b>B 等待 [0,50] 与 [180,200]</b></td></tr></table>',
 traps:['<b>「CPU 空闲」与「进程等待」不是一回事</b>：0–50ms 时 CPU 满载但 B 在等；'
       +'100–150ms 时无人等 CPU 但 CPU 闲着。<b>两个概念互不蕴含。</b>',
       '<b>最常见的丢分是漏掉第二段等待</b>：B 的输入 180ms 就完了，但 CPU 被 A 占到 200，又干等 20ms。',
       '<b>利用率的分母永远是「从开始到全部结束的总时长」</b>，不是某个程序的长度、也不是 CPU 段之和。'],
 qids:['1.2-21','1.2-22']},

{id:'TO1-11', ch:1, tier:'🟡', title:'五种 OS 结构：先问「这句话本来是谁的标签」',
 trig:['分层式','模块化','宏内核','微内核','外核','结构清晰且便于调试','机制与策略分离'],
 flow:'<b>本节唯一的出题手法是<u>张冠李戴</u></b>（把 A 结构的优点安到 B 头上，书答自己都写了"属于移花接木"）。'
     +'做法：把五个标签背死，读到某句话先问「它本来是谁的」。',
 tmpl:'<table class="tk"><tr><th>结构</th><th>招牌标签</th><th>硬伤</th></tr>'
     +'<tr><td><b>分层</b></td><td><b>单向依赖 · 便于调试与验证</b>（有可靠的开发顺序）</td><td>层次划分难 · <b>穿层 ⟹ 效率低</b> · 依赖僵化</td></tr>'
     +'<tr><td><b>模块化</b></td><td><b>高内聚低耦合 · 支持并行开发 · 模块间直接调用效率高</b></td><td><b>无可靠的决定顺序（难调试）</b> · 一模块崩全内核崩</td></tr>'
     +'<tr><td><b>宏内核</b></td><td><b>性能高</b>（共享地址空间、直接函数调用）</td><td>庞大难移植 · <b>可靠性低</b></td></tr>'
     +'<tr><td><b>微内核</b></td><td><b>可靠 · 安全 · 可扩展 · 可移植 · 适合分布式</b></td><td><b>性能差</b>（态切换+消息传递+数据拷贝）</td></tr>'
     +'<tr><td><b>外核</b></td><td><b>直接把裸资源分给用户级系统</b>，库 OS 自定制</td><td>只管<b>分配/回收/保护</b>；<b>进程调度仍归内核</b></td></tr></table>'
     +'<p class="note"><b>现状要背</b>：<b>Linux / Windows / Android ＝ 宏内核或混合内核</b>；'
     +'<b>macOS、iOS 的 XNU ＝ Mach 微内核 + BSD 宏内核融合</b>；'
     +'<b>Fuchsia（谷歌）、HarmonyOS NEXT（华为）＝ 微内核</b>。</p>',
 traps:['<b>「微内核 + 性能/效率」一律判错</b>——ch1 用五道题确认过这条（1.4-04/07/08/09 与 2023 真题）。'
       +'速答法：微内核的优点全是"性"字辈里<u>除性能之外</u>的那些。',
       '<b>「微内核结构没有单一内核稳定」是错的</b>：内核服务越少内核越稳定。',
       '<b>Windows 不是分层式</b>，是宏内核与微内核的<b>融合</b>（1.4-08 的 Ⅰ）。',
       '<b>「机制与策略分离」是微内核的原则，「功能分层设计」是分层的</b>——两者最常被对调。'],
 qids:['1.4-01','1.4-03','1.4-04','1.4-05','1.4-07','1.4-08','1.4-09','1.4-10','1.6-21']},

{id:'TO1-12', ch:1, tier:'🟡', title:'引导过程：BIOS → MBR → PBR → 启动管理器 → 内核，每一棒只认识下一棒',
 trig:['引导程序','自举程序','MBR','活动分区','BIOS','操作系统最终被加载到'],
 flow:'<b>背这条七步链，1.5 的五道题全在链条的不同环节上抠。</b><br>'
     +'<b>核心判据：凡是「某某<u>直接</u>加载内核」「启动管理器由 BIOS 直接读取」这类<u>跳环节</u>的说法，一律错。</b><br>'
     +'为什么必须接力？<b>主板 ROM 是厂商烧死的，它不可能预知你装的是哪个 OS、装在哪个分区。</b>',
 tmpl:'<pre class="code">① CPU 从 ROM 读 BIOS 执行（BIOS 还负责建立中断向量表）\n'
     +'② POST 自检 + 按 CMOS 设置选启动设备\n'
     +'③ BIOS 把启动设备首扇区 MBR 读入内存（MBR = 引导代码 + 分区表）\n'
     +'④ MBR 解析分区表 → 找活动分区 → 读入其首扇区 PBR\n'
     +'⑤ PBR 加载启动管理器（GRUB / Bootmgr）\n'
     +'⑥ 启动管理器把内核映象读入内存并跳转执行\n'
     +'⑦ 内核初始化：页表 · PCB链/就绪队列/阻塞队列 · 设备控制块\n'
     +'   最后启动第一个用户态进程（如 init）</pre>'
     +'<table class="tk"><tr><th></th><th>自举程序</th><th>OS 引导程序（启动管理器）</th></tr>'
     +'<tr><td>位置</td><td><b>主板 ROM</b>（BIOS 的一部分）</td><td><b>硬盘上，活动分区的引导扇区</b></td></tr>'
     +'<tr><td>职责</td><td>自检、选启动设备、<b>读 MBR</b></td><td><b>加载 OS 内核映象</b>、多系统选择</td></tr></table>',
 traps:['<b>两个都叫「引导程序」，位置完全不同</b>：问「自举程序在哪个芯片」答 <b>ROM</b>；'
       +'问「OS 的引导程序位于哪」答 <b>硬盘</b>。<b>不矛盾，是两个程序。</b>',
       '<b>CMOS 存的是参数不是程序</b>（启动顺序、系统时间），BIOS 设置界面俗称"CMOS 设置"最容易误导。',
       '<b>引导只把<u>内核</u>装进内存，不是"把 OS 全部装进内存"</b>——常驻内存的只有内核。',
       '<b>2013 真题「OS 最终被加载到」答 RAM</b>：CPU 只能执行主存里的指令，而主存断电即失。'],
 qids:['1.5-11','1.5-12','1.5-13','1.5-14','1.5-15','1.6-19']},

{id:'TO1-13', ch:1, tier:'🟢', title:'虚拟机：一类还是二类，只看「VMM 下面压着谁」',
 trig:['虚拟机管理程序','VMM','裸金属','宿主操作系统','VMware Workstation','虚拟化技术'],
 flow:'<b>判据一句话：VMM 下面是<u>硬件</u> ⟹ 第一类；下面是<u>宿主 OS</u> ⟹ 第二类。</b><br>'
     +'更实用的辨认法：<b>装它之前要不要先装别的 OS？</b>要 ⟹ 第二类（你电脑上的 VMware/VirtualBox）；'
     +'不要 ⟹ 第一类（服务器上的 ESXi、Xen）。',
 tmpl:'<table class="tk"><tr><th></th><th><b>第一类</b>（裸金属）</th><th><b>第二类</b>（寄居）</th></tr>'
     +'<tr><td>位置</td><td>直接跑在硬件上</td><td>宿主 OS 上的一个<b>用户态进程</b></td></tr>'
     +'<tr><td>特权级</td><td><b>系统中唯一处于最高特权级的软件</b></td><td>与普通应用同级</td></tr>'
     +'<tr><td>效率</td><td><b>高</b></td><td>低（要经宿主 OS 转一手）</td></tr>'
     +'<tr><td>典型</td><td>Xen、VMware ESXi</td><td><b>VMware Workstation</b></td></tr></table>'
     +'<p class="note"><b>关键机制</b>：客户 OS 运行在「<b>虚拟内核态</b>」——它<u>以为</u>自己在真实内核态，'
     +'一执行敏感指令就<b>触发异常并陷入 VMM</b>。<b>这与"用户态执行特权指令 ⟹ 陷入内核"是同构的，'
     +'虚拟化不过是把这套把戏又叠了一层。</b></p>',
 traps:['<b>2025 真题</b>：「VMM 与操作系统的特权级<u>相同</u>」<b>错</b>——'
       +'VMM 必须高于客户 OS，否则敏感指令没人拦截，虚拟机之间的隔离立刻崩塌。',
       '<b>「实现 VMM 的代码量大于一个完整 OS」错</b>：VMM 只模拟硬件接口，'
       +'不实现文件系统/调度/GUI/协议栈。<b>"位置在下面"不等于"功能更复杂"。</b>',
       '<b>「虚拟机中只能安装一种 OS」错</b>；<b>「多台虚拟机实现了真正的并行」也错</b>（是并发）。',
       '<b>虚拟磁盘实际上是宿主机上的一个普通文件</b>（书上原话），这是理解虚拟化的关键直觉。'],
 qids:['1.6-22','1.6-18','1.6-17','1.6-16']},

{id:'TO1-14', ch:1, tier:'🟡', title:'【元套路】ch1 全是概念题——先扫「绝对化措辞」和「同源三选项」',
 trig:['所有','一定','只能','唯一','必须','不正确的是','错误的是'],
 flow:'<b>第 1 章 88 道题里 86 道是选择题，而且大量是"下列说法哪个错"。</b>'
     +'在真正动用知识之前，有两条纯技术的筛法能先干掉一半选项：<br>'
     +'<b>① 扫绝对化措辞</b>：出现<b>所有 / 任何 / 一定 / 只能 / 全部 / 越…越…</b>时，先找反例。<br>'
     +'<b>② 找同源三选项</b>：若有三个选项在说同一件事的三种说法，正解往往是剩下那个。',
 tmpl:'<table class="tk"><tr><th>措辞</th><th>本章的实例</th><th>为什么错</th></tr>'
     +'<tr><td><b>所有</b></td><td>"所有库函数的实现都依赖系统调用"</td><td>strlen 一个反例即倒</td></tr>'
     +'<tr><td><b>全部</b></td><td>"引导程序会将硬盘中的 OS <b>全部</b>加载到内存"</td><td>常驻内存的只有内核</td></tr>'
     +'<tr><td><b>只能</b></td><td>"CPU 处于内核态时<b>只能</b>执行特权指令"</td><td>内核里绝大多数是普通指令</td></tr>'
     +'<tr><td><b>一定</b></td><td>"中断或异常发生时 CPU <b>一定</b>处于内核态"</td><td>可能在用户态（2024）</td></tr>'
     +'<tr><td><b>越…越…</b></td><td>"进程数<b>越多</b> CPU 利用率<b>越高</b>"</td><td>先升后降，会抖动甚至死锁（2022）</td></tr>'
     +'<tr><td><b>统一</b></td><td>"不同 OS 提供<b>统一的</b>系统调用接口"</td><td>各 OS 完全不同（2019）</td></tr></table>'
     +'<p class="note"><b>"同源三选项"的实例</b>：1.1-06 里 A"能敲键盘直接用"、C"是命令接口中的命令"、'
     +'D"与系统命令一样"——三条其实是同一个错误的三种说法，正解必是剩下的 B。</p>',
 traps:['<b>这是筛法不是判据</b>：绝对化措辞也有对的时候，比如'
       +'「<b>从用户态进内核态的<u>唯一</u>途径是中断或异常</b>」「<b>系统调用的被调用过程<u>一定</u>运行在内核态</b>」'
       +'都是<u>正确</u>的绝对化表述。<b>先怀疑、再验证，不要直接判死。</b>',
       '<b>读到句号才判定</b>：本章多个选项是"前半句对、后半句错"（1.4-08 的 Ⅳ：'
       +'"添加系统服务时不必修改内核"对，"使系统更高效"错），<b>任何一处错，整条就错。</b>'],
 qids:['1.1-10','1.5-11','1.3-30','1.3-33','1.2-20','1.3-26','1.4-08']}

];

window.TRICK_META = {
  subject:'操作系统',
  ready:[1],
  note:'第 1 章共 88 道课后题做完后提炼（两道甘特图综合题经 Python 排程复算）。第 2–5 章随建库陆续加入。'
};
